Jenkins:
===========

Day-1
Why Jenkins?
Configure Jenkins in Ubuntu
Create a Job in Jenkins
=============================

 - Needed for building, deploying and automating any project.
 - CICD tool.

Installation:
Take one Ubuntu machine and execute following commands:
Security Group:
	Inbound: port 22, 8080
	Outbound: All Traffic

Change hostname:
	sudo hostnamectl set-hostname jenkins-master
Logout and Login Again.

sudo apt update && sudo apt upgrade -y
sudo apt install fontconfig openjdk-21-jre -y
sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" https://pkg.jenkins.io/debian-stable binary/ | sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins -y
sudo systemctl status jenkins
ps -ef | grep jenkins
sudo systemctl start jenkins
sudo systemctl enable jenkins

Open a browser
Copy the pulic IP 
Open in browser:=> 
	public_ip:8080

Copy the password from:
	sudo cat /var/lib/jenkins/secrets/initialAdminPassword


Day 2
=====================
plugin - pipeline stage view
Agent machine Setup
Integration with git web hook

Worker Setup:
==============
sudo hostnamectl set-hostname worker-1
sudo apt update && sudo apt upgrade -y
apt list openjdk-21*
sudo apt install openjdk-21-jdk -y
java -version

On Master machine:		
	Dashboard > Manage Jenkins > Nodes > New node
		Give a name > Node-1
		Tick Type as Permanent Agent > Click on create
		Add a description
		Number of executors 2
				To be followed:
					No. of Jobs to be executed = 2 times the no. of CPUs
		Remote root directory : /home/ubuntu/
		Labels : Empty
		Usage: Use this node as much as possible - every cicd will use this
		Launch method: Launch agents via SSH
			Host:
				PUBLIC ip OF SLAVE
			Credentials:
				Add:
					Kind: SSH Username with private key
					scope: global
					ID: Node-1
					Username: ubuntu
					Tick - Treat username as secret
					Private Key > Enter directly
						provide the private key for ssh
					Passphrase: Empty
					Add
		Host Key Verification Strategy: Non verifying Verification Strategy
		Availability: Keep this agent online as much as possible
		Save
		Built in Node: 
			Configure > Set Number of executors to 0
		Labels: Add anything
		Usage : Only build jobs with label expressions matching this node	
		
		select the node and bring it online:


Install plugin - pipeline stage view

		Install Git on agent:  sudo apt install git -y
		Install Maven: sudo apt install maven -y

Creating a webhook in GitHub:
==
•	To create a webhook manually, navigate to your GitHub repository, click on Settings, and then click on Webhooks.
•	Click on Add webhook, and provide the Jenkins URL. The Jenkins URL should be in the following format:

 - payload url : http://<jenkins_server>/github-webhook/
 - Ex: http://65.0.4.88:8080/github-webhook/
 - content type - application/json
 - Enable SSL verification

•	Now configure the Jenkins project, Check the GitHub hook trigger button.
 - tick - GitHub hook trigger for GITScm polling

