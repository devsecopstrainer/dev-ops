Jenkins:
===========

Day-1
Why Jenkins?
Configure Jenkins in Ubuntu
Create a Job in Jenkins
=============================

 - Needed for building, deploying and automating any project.
 - CICD tool.

Installation:
Take one Ubuntu machine and execute following commands:
Security Group:
	Inbound: port 22, 8080
	Outbound: All Traffic

Change hostname:
	sudo hostnamectl set-hostname jenkins-master
Logout and Login Again.

sudo apt update && sudo apt upgrade -y
sudo apt install fontconfig openjdk-21-jre -y
sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" https://pkg.jenkins.io/debian-stable binary/ | sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update
sudo apt install jenkins -y
sudo systemctl status jenkins
ps -ef | grep jenkins
sudo systemctl start jenkins
sudo systemctl enable jenkins

Open a browser
Copy the pulic IP 
Open in browser:=> 
	public_ip:8080

Copy the password from:
	sudo cat /var/lib/jenkins/secrets/initialAdminPassword


Day 2
=====================
plugin - pipeline stage view
Agent machine Setup
Integration with git web hook

Worker Setup:
==============
sudo hostnamectl set-hostname worker-1
sudo apt update && sudo apt upgrade -y
apt list openjdk-21*
sudo apt install openjdk-21-jdk -y
java -version

On Master machine:		
	Dashboard > Manage Jenkins > Nodes > New node
		Give a name > Node-1
		Tick Type as Permanent Agent > Click on create
		Add a description
		Number of executors 2
				To be followed:
					No. of Jobs to be executed = 2 times the no. of CPUs
		Remote root directory : /home/ubuntu/
		Labels : Empty
		Usage: Use this node as much as possible - every cicd will use this
		Launch method: Launch agents via SSH
			Host:
				PUBLIC ip OF SLAVE
			Credentials:
				Add:
					Kind: SSH Username with private key
					scope: global
					ID: Node-1
					Username: ubuntu
					Tick - Treat username as secret
					Private Key > Enter directly
						provide the private key for ssh
					Passphrase: Empty
					Add
		Host Key Verification Strategy: Non verifying Verification Strategy
		Availability: Keep this agent online as much as possible
		Save
		Built in Node: 
			Configure > Set Number of executors to 0
		Labels: Add anything
		Usage : Only build jobs with label expressions matching this node	
		
		select the node and bring it online:


Install plugin - pipeline stage view

		Install Git on agent:  sudo apt install git -y
		Install Maven: sudo apt install maven -y

Day-3
===================
SonarQube is an open-source platform developed by SonarSource for continuous inspection of code quality. 
Sonar does static code analysis, which provides a detailed report of bugs, code smells, vulnerabilities, code duplications.
It supports 30+ major programming languages through built-in rulesets and can also be extended with various plugins.

Setup:

Take 3 Ubuntu machines,  
	Security grp - 
		Machine			Inbound			Outbound
		Jenkins Master	22, 8080		all traffic		T3 micro, 10 GB memory.
		Node-1			22				all traffic		T3 micro, 10 GB memory.
		SonarQube		22, 9000		all traffic		T2 Medium, 12 gb

Sonar Qube set-up:
sudo hostnamectl set-hostname sonar
sudo apt update && sudo apt upgrade -y
Java is required to run sonarqube.
sudo apt install openjdk-21-jdk -y
mkdir sonarqube
cd sonarqube
sudo apt install unzip -y
wget https://binaries.sonarsource.com/Distribution/sonarqube/sonarqube-26.3.0.120487.zip
unzip sonarqube-26.3.0.120487.zip
rm -rf sonarqube-26.3.0.120487.zip
mv sonarqube-26.3.0.120487 sonarqube
cd sonarqube-26
pwd: /home/ubuntu/sonarqube/sonarqube-26/bin/linux-x86-64
Run: ./sonar.sh console
login to sonar through UI:
public_ip:9000
username and password = admin

Create a local project
Locally
Analyze "idream-microservice": sqp_4ea1c78e11eb10542a2e519a37d22c40a78f95d4

mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
  -Dsonar.projectKey=idream-microservice \
  -Dsonar.projectName='idream-microservice' \
  -Dsonar.host.url=http://13.233.5.205:9000 \
  -Dsonar.token=sqp_4ea1c78e11eb10542a2e519a37d22c40a78f95d4
  
  

Install sonarqube scanner plugin in jenkins




pipeline {
    agent any

    stages {
        stage('git') {
            steps {
                git 'https://github.com/devsecopstrainer/idream-microservice.git'
            }
        }
        stage('Hello') {
            steps {
                mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
                -Dsonar.projectKey=idream-microservice \
                -Dsonar.projectName='idream-microservice' \
                -Dsonar.host.url=http://13.233.5.205:9000 \
                -Dsonar.token=sqp_4ea1c78e11eb10542a2e519a37d22c40a78f95d4
            }
        }
    }
}











Day-4
===================
Nexus: Centralized artifact management.

Take 3 Ubuntu machines,  
	Security grp - 
		Machine			Inbound			Outbound
		Jenkins Master	22, 8080		all traffic		T3 micro, 12 GB memory.
		Node-1			22				all traffic		T3 micro, 12 GB memory.
		Nexus		22, 9000		all traffic		T2 Medium, 12 gb

Search for Download Nexus on Google and open the first link



https://cdn.download.sonatype.com/repository/downloads-prod-group/3/nexus-3.90.1-01-linux-x86_64.tar.gz
https://cdn.download.sonatype.com/repository/downloads-prod-group/3/nexus-3.90.1-01-linux-aarch_64.tar.gz


https://download.sonatype.com/nexus/3/nexus-3.90.2-06-linux-x86_64.tar.gz


Change hostname:
	sudo hostnamectl set-hostname nexus-vm
Logout and Login Again.

sudo apt update && sudo apt upgrade -y
go to /opt
switch to root
wget https://download.sonatype.com/nexus/3/nexus-3.90.2-06-linux-x86_64.tar.gz

tar -xzvf nexus-3.90.2-06-linux-x86_64.tar.gz
rm -rf nexus-3.90.2-06-linux-x86_64.tar.gz
mv nexus-3.90.2-06/ nexus

useradd nexus
tail -n 5 /etc/passwd
tail -n 5 /etc/group

Change the ownership of nexus files to the newly created user - nexus

chown -R nexus:nexus /opt/nexus
chown -R nexus:nexus /opt/sonatype-work/
ll

Edit nexus.rc file present under bin directory of nexus
cd nexus/bin/

Create the file: nexus.rc
add this:
run_as_user="nexus"

chown -R nexus:nexus nexus.rc


go to: /etc/systemd/system
Create a nexus system unit file to auto start nexus server on startup 
https://help.sonatype.com/en/run-as-a-service.html

touch nexus.service
========================

[Unit]
Description=nexus service
After=network.target

[Service]
Type=forking
LimitNPROC=65536
LimitNOFILE=65536
ExecStart=/opt/nexus/bin/nexus start
ExecStop=/opt/nexus/bin/nexus stop

User=nexus
Restart=on-abort
TimeoutSec=600

[Install]
WantedBy=multi-user.target

============

Add nexus service to boot and start nexus
sudo systemctl daemon-reload
sudo systemctl enable nexus.service
sudo systemctl start nexus.service

go to:
/opt/sonatype-work/nexus3/log
tail -f nexus.log

http://13.234.17.138:8081/

user name admin
pwd: cat /opt/sonatype-work/nexus3/admin.password

===========
settings > repository > create repository > maven2 hosted


Update pom.xml file, add distributionManagement tag


<distributionManagement>
  <repository>
    <id>nexus</id>
    <name>testrepo</name>
    <url>http://13.234.17.138:8081/repository/testrepo/</url>
  </repository>
  <snapshotRepository>
    <id>nexus</id>
    <name>testrepo</name>
    <url>http://13.234.17.138:8081/repository/testrepo/</url>
  </snapshotRepository>
</distributionManagement>



In jenkins:
cd /etc/maven
sudo vi settings.xml


update servers tag:

  <servers>

    <server>
      <id>nexus</id>
      <username>admin</username>
      <password>1q2w3e4r!Q@W#E$R</password>
    </server>

  </servers>
















